version: '3'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    restart: always
    networks:
      - urbex_network

networks:
  urbex_network:
    driver: bridge